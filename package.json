{
  "name": "chatifier",
  "version": "0.0.1",
  "description": "Chatifier is taking over the world",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "repository": {
    "type": "git",
    "url": "https://github.com/rsteenbergen/chatifier"
  },
  "author": "Roderik Steenbergen",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/rsteenbergen/chatifier/issues"
  },
  "homepage": "https://github.com/rsteenbergen/chatifier", 
  "devDependencies": {
    "http-server": "^0.6.1"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8000"
  }  
}
